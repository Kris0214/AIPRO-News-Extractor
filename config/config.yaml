# AIPRO News Processing Configuration

# database connection configuration
database:
  host: "sndmp-scan.fbs100.campus"
  port: "5211"
  service_name: "SNDMP_USER_DS"
  oracle_client_path: "D:\\Python\\AIPRO-News-Extractor\\instantclient_23_9"

# Azure OpenAI configuration
azure_openai:
  api_version: "2024-12-01-preview"
  endpoint: "https://oai-ftw-jpe-sit-sandbox-01.openai.azure.com/"
  model: "gpt-4o"
  max_tokens: 5000
  temperature: 0.1

# news processing configuration
news:
  num_workers: 8  # number of parallel processing threads
  timeout: 60  # timeout for a single request (seconds)

# path configuration
paths:
  outputs_dir: "./outputs"
  logs_dir: "./logs"

# logging configuration
logging:
  level: "INFO"
  format: "%(asctime)s - %(levelname)s - %(message)s"
  file_prefix: "aipro_news"
